import { SafeAppProvider } from '@safe-global/safe-apps-provider';
export declare class SafeProvider extends SafeAppProvider {
    request(request: {
        method: string;
        params?: any[];
    }): Promise<any>;
}
